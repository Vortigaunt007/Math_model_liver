#include <iostream>
#include "solver.h"
#include <fstream>

using namespace std;

int main()
{
    /*
    double k1 = 1.0, k2 = 1.0;
    int N;
    std::ofstream fout01("Test1.txt");

    //for (int i = 5; i < 80; i++) {
    N = 80;
    k1 = 10.0; k2 = 1.0;
    Solver S = Solver(N, N, 1.0 / (N - 1), 1.0 / (N - 1), false);

    std::cout << "**********************" << std::endl;
    //std::cout << " *    i = " << i << std::endl;
    std::cout << "**********************" << std::endl;
    //S.CraigMethod(1, fout01, 1, k1, k2);
    S.SymmetrizedConjugateGradients(1, fout01, 1, k1, k2);
    //}
    fout01.close();
    */

    int N;


    return 0;
}
